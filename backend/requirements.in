# Canli backend bagimliliklari (elle duzenlenen liste).
# requirements.txt bundan uretiliyor; hash'li ve tum alt paketler sabit:
#   uv pip compile requirements.in --universal --python-version 3.12 --generate-hashes -o requirements.txt
# Tedarik zinciri: her kurulumda ayni dosyalar, degisen bir paket hash'ten reddedilir.

# Web cercevesi. Eklentisiz: fastapi[standard] kullanilmayan ~15 paket
# (cloud-cli, sentry, jinja, websockets...) getiriyordu.
fastapi==0.141.1
# Starlette < 1.3.1: bilinen DoS aciklari (CVE-2025-62727 vb.).
starlette>=1.3.1
# Docker'da calistirmak icin; Vercel kendi calisma zamanini kullaniyor.
uvicorn==0.54.0

# Overpass istemcisi
httpx==0.28.1
tenacity==9.1.4

# Mesafe hesabi (app/geo.py)
geopy==2.5.0

# Ilce poligonlari (app/districts.py)
shapely==2.0.6

# Disa aktarim: Excel ve PDF
openpyxl==3.1.5
reportlab==5.0.1

# Veri dogrulama
pydantic==2.10.5
pydantic-settings==2.7.1

# Veritabani
sqlalchemy[asyncio]==2.0.37
aiosqlite==0.20.0
asyncpg==0.31.0

# Istek siniri
slowapi==0.1.9

# Izleme
prometheus-client==0.21.1
psutil==6.1.1
