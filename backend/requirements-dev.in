# Testler ve veri isleri (ingest / Overture). Canli sunucuya yuklenmiyor.
#   uv pip compile requirements-dev.in --universal --python-version 3.12 --generate-hashes -o requirements-dev.txt
-c requirements.txt
-r requirements.in
pytest==9.0.3
pytest-asyncio>=1.2
pytest-cov==6.0.0
ruff==0.9.1
duckdb==1.5.5
